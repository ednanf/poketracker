// This file defines a custom errors class for database-related errors.
// It doesn't need any additional properties or methods as it is only boilerplate code.
import { CustomError } from './index.js';
// Custom errors class for database-related errors.
class DatabaseError extends CustomError {}

export default DatabaseError;
